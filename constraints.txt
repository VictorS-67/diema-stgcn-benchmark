# The environment the reported numbers were measured in.
#
# Python 3.11 on Linux, one RTX 4090 (driver 580.173.02), torch 2.7.1 built
# against CUDA 12.8. These are the exact versions of this project's runtime and
# test dependencies, and of everything they pull in, as they were installed for
# the training campaign. pyproject.toml keeps loose lower bounds so the package
# stays installable next to other work; this file is for matching the table.
#
# Use it as a constraints file, after installing the matching torch build:
#
#   pip install torch==2.7.1 --index-url https://download.pytorch.org/whl/cu128
#   pip install -e ".[dev]" -c constraints.txt
#
# Create the environment with python=3.11, the version the numbers were
# measured on. torch's own CUDA libraries (nvidia-*) and triton are left to
# torch, which pins them exactly for the build you chose.
# Constraints only ever narrow versions, so jupyter from the dev extra and any
# platform-only dependency resolve normally.
#
# pybvh and pybvh-ml were editable checkouts a few commits past the releases
# pinned here. The commits in between are documentation, tests, and a guard
# for retargeted datasets; none of them touches what the recipe runs.
absl-py==2.4.0
contourpy==1.3.3
coverage==7.13.5
cycler==0.12.1
filelock==3.25.2
fonttools==4.62.1
fsspec==2026.3.0
grpcio==1.80.0
iniconfig==2.3.0
jinja2==3.1.6
kiwisolver==1.5.0
lightning-utilities==0.15.3
markdown==3.10.2
markupsafe==3.0.3
matplotlib==3.10.8
mpmath==1.3.0
networkx==3.6.1
numpy==2.4.3
packaging==25.0
pandas==3.0.1
pillow==12.1.1
pluggy==1.6.0
protobuf==7.34.1
pybvh==0.8.2
pybvh-ml==0.6.0
pygments==2.19.2
pyparsing==3.3.2
pytest==9.0.2
pytest-cov==7.1.0
python-dateutil==2.9.0.post0
pytorch-lightning==2.6.1
pyyaml==6.0.3
setuptools==80.10.2
six==1.17.0
sympy==1.14.0
tensorboard==2.20.0
tensorboard-data-server==0.7.2
torch==2.7.1
torchmetrics==1.9.0
tqdm==4.67.3
typing-extensions==4.15.0
werkzeug==3.1.7
